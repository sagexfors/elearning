<div class="card h-100 shadow-sm">
  <div class="card-body">
    <h5 class="card-title"><%= link_to truncate(lesson.name, length: 50), subject_lesson_path(lesson.subject, lesson), class: "card-link stretched-link" %></h5>
    <p class="card-text"><%= truncate(lesson.description, length: 100) %></p>
  </div>
</div>
